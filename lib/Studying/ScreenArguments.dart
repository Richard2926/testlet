class ScreenArguments {
  var docNo ;
  int tno;
  bool some;
  bool editName;
  bool status;
  String name;
  String description;
  String creator;
  //final String message;

  ScreenArguments(this.docNo, this.tno, this.some, this.editName, this.status, this.name, this.description, this.creator);
}